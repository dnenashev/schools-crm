# Schools CRM

Система управления взаимоотношениями со школами (CRM) для работы с образовательными учреждениями. Приложение предоставляет инструменты для отслеживания контактов, встреч, мероприятий и других активностей со школами, а также аналитический дашборд с метриками.

## 🎯 Основные возможности

### Дашборд (`/`)
- **Визуализация метрик** по периодам (дни, недели, месяцы)
- **Интерактивные колонки** с раскрытием недель внутри месяцев
- **12 типов метрик**:
  - Новые школы
  - Школы в работе (накопительная)
  - Контакт состоялся
  - Встреча назначена/состоялась
  - Мероприятие назначено/проведено
  - Выезд на кампус
  - Контакты родителей
  - Загружено в CRM
  - Квалифицированные лиды
  - Доехали до кампуса
  - Предварительные встречи
- **Переход к детализации** — клик по метрике открывает список школ

### Страница школ (`/schools`)
- **Таблица всех школ** с фильтрацией по метрикам и периодам
- **Детальная информация** о каждой школе
- **Фильтрация через URL-параметры** для глубоких ссылок
- **Обновление данных** в реальном времени

### История версий (`/versions`)
- **Автоматические резервные копии** при каждом сохранении
- **Просмотр всех версий** с датами и метаданными
- **Восстановление любой версии** одним кликом
- **Хранение последних 50 версий**

## 🏗️ Архитектура

### Frontend
- **React 18** с TypeScript
- **React Router** для навигации
- **Vite** как сборщик
- **Tailwind CSS** для стилизации
- **REST API** для взаимодействия с бэкендом

### Backend
- **Express.js** сервер на Node.js
- **MongoDB** для хранения данных в production
- **JSON файлы** для локальной разработки (sandbox режим)
- **JWT** авторизация с bcrypt хэшированием паролей
- **Автоматические версии** (последние 50 сохраняются)
- **CORS** с настраиваемым allowlist

### Структура данных

#### Школа (School)
```typescript
{
  id: string
  name: string
  district: string
  region: 'Москва' | 'Московская область'
  city: string
  address: string
  website: string
  uchiLink: string
  travelTime: string
  tags: string[]
  
  // Статусы с датами
  inWorkDate: string | null
  contactDate: string | null
  meetingScheduledDate: string | null
  meetingHeldDate: string | null
  eventScheduledDate: string | null
  eventHeldDate: string | null
  campusVisitPlannedDate: string | null
  loadedToCRMDate: string | null
  qualifiedLeadDate: string | null
  arrivedToCampusDate: string | null
  preliminaryMeetingDate: string | null
  
  // Дополнительные поля
  callbackDate: string | null
  notes: string
  amoLink: string
  
  // История активностей
  activities: Activity[]
}
```

#### Активность (Activity)
```typescript
{
  id: string
  date: string
  type: 'contact' | 'meeting' | 'event' | 'call' | 'note' | 'campus_visit'
  description: string
  parentContacts?: number
  classesContacted?: string[]
}
```

## 📦 Установка

```bash
# Установка зависимостей
npm install
```

## 🚀 Запуск

### Разработка (фронтенд + бэкенд одновременно)
```bash
npm run dev:all
```

Это запустит:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001

### Отдельно

**Только фронтенд:**
```bash
npm run dev
```

**Только бэкенд:**
```bash
npm run server
```

### Production сборка
```bash
npm run build
npm run preview
```

## 🚀 Деплой в Production

Проект готов к деплою с использованием следующей архитектуры:

- **Frontend**: Vercel (статика Vite)
- **Backend**: Render/Railway/Fly.io (Express.js)
- **Database**: MongoDB Atlas

### Шаг 1: Настройка MongoDB Atlas

1. Создайте бесплатный кластер на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте пользователя базы данных (Database Access)
3. Добавьте `0.0.0.0/0` в Network Access (или IP вашего сервера)
4. Скопируйте connection string: `mongodb+srv://user:password@cluster.mongodb.net/schools_crm`

### Шаг 2: Миграция данных

Перенесите данные из JSON файлов в MongoDB:

```bash
# Предварительный просмотр (без записи)
MONGODB_URI="mongodb+srv://..." npm run migrate:mongodb:dry

# Миграция с хэшированием паролей (рекомендуется)
MONGODB_URI="mongodb+srv://..." npm run migrate:mongodb:hash

# Только миграция (без хэширования)
MONGODB_URI="mongodb+srv://..." npm run migrate:mongodb
```

### Шаг 3: Деплой Backend на Render

1. Создайте новый Web Service на [Render](https://render.com)
2. Подключите GitHub репозиторий
3. Настройки:
   - **Build Command**: `npm ci`
   - **Start Command**: `node server/index.js`
   - **Root Directory**: оставить пустым
4. Добавьте Environment Variables:
   ```
   NODE_ENV=production
   MONGODB_URI=mongodb+srv://...
   JWT_SECRET=<сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
   CORS_ORIGIN=https://your-frontend.vercel.app
   ```

### Шаг 4: Деплой Frontend на Vercel

1. Импортируйте проект на [Vercel](https://vercel.com)
2. Настройки сборки (автоматически определятся):
   - **Framework**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
3. Добавьте Environment Variable:
   ```
   VITE_API_URL=https://your-backend.onrender.com/api
   ```

### Environment Variables

#### Backend (Render/Railway)

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `MONGODB_URI` | ✅ | MongoDB connection string |
| `JWT_SECRET` | ✅ | Секрет для JWT токенов (минимум 32 символа) |
| `CORS_ORIGIN` | ✅ | URL фронтенда (например: `https://app.example.com`) |
| `PORT` | ❌ | Автоматически устанавливается PaaS |
| `NODE_ENV` | ❌ | `production` для прода |

#### Frontend (Vercel)

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `VITE_API_URL` | ✅ | URL API бэкенда (например: `https://api.example.com/api`) |

### Health Check

API предоставляет endpoint для мониторинга:

```
GET /api/health
```

Ответ:
```json
{
  "status": "ok",
  "mode": "production",
  "timestamp": "2026-01-27T10:00:00.000Z"
}
```

Используйте этот endpoint для UptimeRobot или аналогичных сервисов.

## 📁 Структура проекта

```
Schools-crm/
├── src/                    # Исходный код фронтенда
│   ├── components/         # React компоненты
│   │   ├── Dashboard.tsx   # Компонент дашборда
│   │   ├── SchoolCard.tsx  # Карточка школы
│   │   ├── AuthProvider.tsx # Провайдер авторизации
│   │   └── ModeProvider.tsx # Провайдер режима (prod/sandbox)
│   ├── config/
│   │   └── api.ts         # Конфигурация API и режимов
│   ├── pages/              # Страницы приложения
│   │   ├── DashboardPage.tsx
│   │   ├── SchoolsPage.tsx
│   │   └── VersionsPage.tsx
│   ├── types/              # TypeScript типы
│   │   └── school.ts
│   └── App.tsx             # Главный компонент
├── server/                 # Backend сервер
│   ├── index.js           # Express сервер
│   ├── db.js              # MongoDB слой доступа
│   └── data/              # Данные (для sandbox режима)
│       ├── schools.json   # Основной файл данных
│       ├── users.json     # Пользователи
│       ├── plans.json     # Планы
│       └── backups/       # Резервные копии
├── scripts/               # Утилиты
│   ├── convert-csv-to-json.js   # Конвертация CSV в JSON
│   ├── migrate-to-mongodb.js    # Миграция в MongoDB
│   └── migrate-schools.js       # Миграция данных школ
├── data/                  # Исходные данные
│   └── uchi_schools_data_for_work.csv
├── .env.example           # Пример переменных окружения
└── package.json
```

## 🔌 API Endpoints

### Школы
- `GET /api/schools` — получить все школы
- `GET /api/schools/:id` — получить школу по ID
- `PUT /api/schools/:id` — обновить школу
- `PUT /api/schools/:id/status` — быстрое обновление статуса
- `POST /api/schools/:id/activity` — добавить активность

### Метрики
- `GET /api/metrics?from=YYYY-MM-DD&to=YYYY-MM-DD` — получить метрики за период

### Версии
- `GET /api/versions` — список всех версий
- `POST /api/restore/:version` — восстановить версию

## 🛠️ Утилиты

### Конвертация CSV в JSON
```bash
node scripts/convert-csv-to-json.js
```

Конвертирует `data/uchi_schools_data_for_work.csv` в `src/data/schools.json`

## 🔒 Безопасность данных

- **Автоматические бэкапы** при каждом сохранении
- **Хранение последних 50 версий**
- **Безопасное восстановление** — текущие данные сохраняются перед откатом
- **Локальное хранение** — все данные хранятся в файловой системе

## 🎨 Технологии

- **Frontend:**
  - React 18.2.0
  - TypeScript 5.3.0
  - Vite 5.0.0
  - React Router DOM 7.13.0
  - Tailwind CSS 3.4.0

- **Backend:**
  - Express 5.2.1
  - MongoDB 7.0 (драйвер)
  - JWT (jsonwebtoken 9.0.2)
  - bcryptjs 3.0.3
  - CORS 2.8.6
  - Node.js (ES Modules)

- **Деплой:**
  - Vercel (frontend)
  - Render/Railway (backend)
  - MongoDB Atlas (database)

- **Инструменты:**
  - Concurrently 9.2.1 (для параллельного запуска)
  - Playwright (E2E тесты)

## 📝 Лицензия

Проект для внутреннего использования.

## 🔄 Обновления

При каждом сохранении данных:
1. Создаётся резервная копия в `server/data/backups/`
2. Обновляется основной файл `server/data/schools.json`
3. Старые бэкапы (свыше 50) автоматически удаляются

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2026
